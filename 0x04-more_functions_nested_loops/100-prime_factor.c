#include <stdio.h>
#include "main.h"

/**
 * main - main block
 * Description: Find and print the largest prime facto of the number
 * 612852475143
 * Return: 0
 * got the answer but didn't get it -perhaps we will understand it later-
 * int main(void)
{
	int c;
	long num = 612852475143;

	for (c = (int) sqrt(num); c > 2; c++)
	{
		if (num % c == 0)
		{
			printf("%d\n", c);
			break;
		}
	}

	return (0);
}
credit goes to > Bright Daniel
*/

